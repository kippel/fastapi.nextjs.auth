# FastAPI Next js Auth

```
$ python3 -m venv .venv
$ source .venv/bin/activate


$ pip install "fastapi[standard]" sqlalchemy python-jose bcrypt cryptography passlib pyyaml

backend$ fastapi run main.py --port 4000

```
